---
apiVersion: batch/v1
kind: Job
metadata:
  labels:
    app: wordsolver
    chart: "{{ $.Chart.Name }}-{{ $.Chart.Version }}"
    release: {{ $.Release.Name | quote }}
    heritage: {{ $.Release.Service | quote }}
    version: {{ $.Chart.Version }}
  {{- $str := "producer" | lower}}
  {{- $str = print $str $.Values.completions}}
  {{- $str = print $str $.Values.parallelism}}
  {{- $str = print $str "-" $.Values.producerImage.tag }}
  {{- $str = print $str $.Values.inputs }}
  {{- $str = print $str | adler32sum  }}
  name: {{ $str | quote}}
  namespace: wordsolver
spec:
  completions: {{$.Values.completions}}
  parallelism: {{$.Values.parallelism}}
  backoffLimit: 2
  template:
    spec:
    spec:
      containers:
      - image: "{{$.Values.producerImage.repository }}:{{$.Values.producerImage.tag | toString }}"
        imagePullPolicy: {{ $.Values.pullPolicy }}
        name: {{ $.Values.producerImage.repository | toString | replace "/" "-" | replace "_" "-"  | adler32sum}}-{{ $.Values.producerImage.tag | toString | replace "." "-"  | adler32sum }}
        resources: 
          limits:
            cpu: 500m 
            memory: 5Mi
          requests:
            cpu: 10m
            memory: 5Mi
        env:
          - name: "INPUTS"
            value: {{$.Values.inputs | quote}}
      restartPolicy: Never
      
---
apiVersion: batch/v1
kind: Job
metadata:
  labels:
    app: wordsolver
    chart: "{{ $.Chart.Name }}-{{ $.Chart.Version }}"
    release: {{ $.Release.Name | quote }}
    heritage: {{ $.Release.Service | quote }}
    version: {{ $.Chart.Version }}
  {{- $str := "processor" | lower}}
  {{- $str = print $str $.Values.completions}}
  {{- $str = print $str $.Values.parallelism}}
  {{- $str = print $str "-" $.Values.processorImage.tag }}
  {{- $str = print $str $.Values.inputs }}
  {{- $str = print $str $.Values.reportFreq }}
  {{- $str = print $str | adler32sum  }}
  name: {{ $str | quote}}
  namespace: wordsolver
spec:
  completions: {{$.Values.completions}}
  parallelism: {{$.Values.parallelism}}
  backoffLimit: 2
  template:
    spec:
    spec:
      containers:
      - image: "{{$.Values.processorImage.repository }}:{{$.Values.processorImage.tag | toString }}"
        imagePullPolicy: {{ $.Values.pullPolicy }}
        name: {{ $.Values.processorImage.repository | toString | replace "/" "-" | replace "_" "-"  | adler32sum}}-{{ $.Values.processorImage.tag | toString | replace "." "-"  | adler32sum }}
        resources: 
          limits:
            cpu: 500m 
            memory: 5Mi
          requests:
            cpu: 10m
            memory: 5Mi
        env:
          - name: "REPORT_FREQ"
            value: {{$.Values.reportFreq | quote}}
      restartPolicy: Never
      
---
apiVersion: batch/v1
kind: Job
metadata:
  labels:
    app: wordsolver
    chart: "{{ $.Chart.Name }}-{{ $.Chart.Version }}"
    release: {{ $.Release.Name | quote }}
    heritage: {{ $.Release.Service | quote }}
    version: {{ $.Chart.Version }}
  {{- $str := "resultwriter" | lower}}
  {{- $str = print $str $.Values.completions}}
  {{- $str = print $str $.Values.parallelism}}
  {{- $str = print $str "-" $.Values.resultprinterImage.tag }}
  {{- $str = print $str $.Values.inputs }}
  {{- $str = print $str $.Values.reportFreq }}
  {{- $str = print $str | adler32sum  }}
  name: {{ $str | quote}}
  namespace: wordsolver
spec:
  completions: {{$.Values.completions}}
  parallelism: {{$.Values.parallelism}}
  backoffLimit: 2
  template:
    spec:
    spec:
      containers:
      - image: "{{$.Values.resultprinterImage.repository }}:{{$.Values.resultprinterImage.tag | toString }}"
        imagePullPolicy: {{ $.Values.pullPolicy }}
        name: {{ $.Values.resultprinterImage.repository | toString | replace "/" "-" | replace "_" "-"  | adler32sum}}-{{ $.Values.resultprinterImage.tag | toString | replace "." "-"  | adler32sum }}
        resources: 
          limits:
            cpu: 500m 
            memory: 5Mi
          requests:
            cpu: 10m
            memory: 5Mi
        env:
          - name: "INPUTS"
            value: {{$.Values.inputs | quote}}
          - name: "REPORT_FREQ"
            value: {{$.Values.reportFreq | quote}}
      restartPolicy: Never
      
