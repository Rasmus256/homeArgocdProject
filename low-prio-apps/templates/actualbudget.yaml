apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: actualbudget
  namespace: argocd
  labels:
    "category": "{{$.Values.spec.labels.category}}"
spec:
  destination:
    namespace: actualbudget
    server: 'https://kubernetes.default.svc'
  source:
    path: low-prio-apps/actualbudget
    repoURL: 'git@github.com:Rasmus256/homeArgocdProject.git'
    targetRevision: HEAD
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
